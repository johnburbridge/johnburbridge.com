/**
 * Build file for johnburbridge.com
 */

apply plugin: 'groovy';


allprojects {

    // if the upload authorized username / password weren't passed via the -P switch, 
    // then use the read-only account defined here as defaults
    repositoryUsername = project.hasProperty('repoUsername') ? repoUsername : 'build';
    repositoryPassword = project.hasProperty('repoPassword') ? repoPassword: 'build';

    repositories {
        mavenRepo url: repositoryUrl;
    }
}
